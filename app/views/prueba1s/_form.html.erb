<%= form_for(@prueba1) do |f| %>
  <% if @prueba1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prueba1.errors.count, "error") %> prohibited this prueba1 from being saved:</h2>

      <ul>
      <% @prueba1.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
